# Migration 063 — RESERVED (never created)

**Status:** number reserved during branch development, never landed.

The 063 slot was allocated on a feature branch that was abandoned before
merge. Migration numbering is monotonic; the gap is intentional and the
checker (`scripts/check-migrations.js`) tolerates documented gaps via this
README's presence.

If you need the next number, use the highest existing + 1. Do NOT reuse 063.

Audit reference: 2026-05-18 M2 finding.
